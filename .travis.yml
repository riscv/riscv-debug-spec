git:
  submodules: false

language: latex

addons:
  apt:
    packages:
      - make
      - python
      - python-sympy
      - graphviz
      - texlive-full
      - texlive-latex-extra
      - python-pygments
      
script:
  - make

