git:
  submodules: false

language: latex

before_install:
- sudo apt-get -qq update && \
    sudo apt-get install -y --no-install-recommends \
    make \
    python \
    python-sympy \
    graphviz \
    texlive-full \
    python-pygments 

script:
- make

