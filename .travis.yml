git:
  submodules: false

language: latex

addons:
  apt:
    packages:
      - make
      - python
      - python-sympy
      - graphviz
      - texlive-full
      - python-pygments

install:
 - tlmgr install minted

script:
  - make

